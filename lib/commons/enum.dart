enum AuthStatus {
  loadingResources,
  authenticated,
  unregistered,
  unauthenticated,
  nonExistent
}

enum UserStatus {
  loading,
  signedOut,
  forcedSignedOut,
  active,
}

enum DeviceType { mobile, desktop, indeterminate }
